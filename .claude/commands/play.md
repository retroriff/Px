You are now in **Music Creation Mode** for Px live coding.

Read the agent instructions immediately:

```bash
cat "Docs/how-to-play-music/agent-instructions.md"
```

After reading the instructions, you will know how to use `./play.sh` to execute music commands directly.
